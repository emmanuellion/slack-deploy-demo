# Exemple de variables lues par scripts/deploy.sh si vous le souhaitez
# (Sinon, passez par GitHub Secrets et les inputs du workflow)

# API_KEY_BACKEND="xxx"
# DEPLOY_SSH_HOST="deploy@your-host"
# DEPLOY_PATH="/var/www/app"
